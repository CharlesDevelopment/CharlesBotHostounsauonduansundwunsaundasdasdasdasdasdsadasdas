const Discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    let pingembed = Discord.MessageEmbed()
    .setColor('RANDOM')
    .setDescription=(`Tes Ping ea ${m.createdTimstamp - message.createdTimstamp}`)
}

module.exports.config = {
    name: "pingpong",
    description: "example of an Embed.",
    usage: "/ping2",
    accessableby: "Members",
    aliases: []
}
